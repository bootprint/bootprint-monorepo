## Usage

Consider a bootprint-module that uses the following Handlebarse-template as `index.html.hbs`

{{include 'examples/templates/index.html.hbs'}}

A test can be written like this: 

{{example 'examples/example-spec.js'}}

The test will run bootprint with the given module and verify the generated HTML 
using the {{npm "cheerio"}} library.

{{{exec '../node_modules/.bin/mocha --recursive example-spec.js' cwd='examples/'}}}
