{
  "name": "customize",
  "version": "0.7.0",
  "description": "A simple framework to create customizable engines",
  "repository": {
    "type": "git",
    "url": "https://github.com/nknapp/customize.git"
  },
  "author": {
    "name": "Nils Knappmeier",
    "email": "npm@knappi.org",
    "url": "https://github.com/nknapp"
  },
  "bugs": {
    "url": "https://github.com/nknapp/customize/issues"
  },
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "pretest": "standard --version || npm -g install standard",
    "test": "mocha && standard",
    "preformat": "standard --version || npm -g install standard",
    "format": "standard --format",
    "thought": "thought run -a",
    "prethoughtcheck": "thought --version || npm -g install thought",
    "thoughtcheck": "thought check-engines",
    "version": "thoughtful.js changelog -o -a && npm run thought",
    "preversion": "npm run thoughtcheck"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "jsonschema": "^1.0.2",
    "jsonschema-extra": "^1.2.0",
    "lodash": "^3.9.3",
    "minimatch": "^3.0.0",
    "q": "^1.4.1",
    "q-deep": "^1.0.1",
    "q-io": "^1.13.1"
  },
  "standard": {
    "ignore": [
      "test/fixtures/**"
    ]
  },
  "files": [
    "lib",
    "index.js",
    "helpers-io.js"
  ],
  "keywords": [],
  "config": {
    "ghooks": {
      "pre-commit": "thoughtful precommit && standard"
    }
  },
  "devDependencies": {
    "chai": "^3.3.0",
    "chai-as-promised": "^5.2.0",
    "ghooks": "^1.0.3",
    "mocha": "^2.3.3",
    "thoughtful-release": "^0.3.0",
    "trace": "^2.3.0"
  }
}
